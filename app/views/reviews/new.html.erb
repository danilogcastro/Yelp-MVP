<%= simple_form_for [@restaurant, @review] do |f| %>
  <%= f.input :rating, as: :numeric, input_html: { min: '0', max: '5', step: '1'} %>
  <%= f.input :content %>
  <%= f.submit %>
<% end %>

<%= link_to "Back to restaurant", restaurant_path(@restaurant) %>
